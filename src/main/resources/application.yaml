spring:
  application:
    name: Music-event-bot
  kafka:
    out:
      telegram-bot-user-received-event:
        topic: telegram-bot-user-received-event


telegram:
  webhookUrl: "https://music-event-bot.duckdns.org"
  bot_username: MusicEvenHelperBot    #use vault
  bot_token: 8223652018:AAERw1pr6gp1ny-SSCO81WMbCvGB6_lV37c   #use vault

management:
  endpoint:
    health:
      probes:
        enabled: true
  health:
    livenessState:
      enabled: true
    readinessState:
      enabled: true